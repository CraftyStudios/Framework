# GitHub Packages credentials (optional):
# You can set these locally in gradle.properties or let GitHub Actions inject env vars USERNAME and TOKEN.
# gpr.user=YOUR_GITHUB_USERNAME
# gpr.key=YOUR_GITHUB_TOKEN
